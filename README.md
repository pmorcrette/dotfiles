# Installation

Attention ce dépôt est basé sur le channel unstable.

```sh
git clone https://github.com/pmorcrette/dotfiles.git ~/.config/home-manager
nix run home-manager/master -- switch --flake ~/.config/home-manager  
```
